{
  "name": "backend",
  "version": "1.0.0",
  "description": "API REST para una tienda de libros con Node.js, Express, MySQL y CORS.",
  "main": "server.js",
  "scripts": {
    "test": "node --check server.js && node --check config/db.js && node --check controllers/books.controller.js && node --check controllers/messages.controller.js && node --check middlewares/errorHandler.js && node --check middlewares/notFound.js && node --check middlewares/validation.js && node --check routes/index.js && node --check routes/books.routes.js && node --check routes/messages.routes.js",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.22.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "nodemon": "^3.1.14"
  }
}
