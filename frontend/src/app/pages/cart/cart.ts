import { Component } from '@angular/core';

// Vista base del carrito de compras.
@Component({
  selector: 'app-cart-page',
  templateUrl: './cart.html',
  styleUrl: './cart.css',
})
export class CartPageComponent {
  // Componente contenedor del carrito.
}
