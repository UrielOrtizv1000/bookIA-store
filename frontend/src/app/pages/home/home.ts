import { Component } from '@angular/core';

// Vista base de la pagina de inicio.
@Component({
  selector: 'app-home-page',
  templateUrl: './home.html',
  styleUrl: './home.css',
})
export class HomePageComponent {
  // Componente inicial de la portada.
}
