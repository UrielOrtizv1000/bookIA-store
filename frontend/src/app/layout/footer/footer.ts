import { Component } from '@angular/core';

// Pie de pagina base de la aplicacion.
@Component({
  selector: 'app-footer',
  templateUrl: './footer.html',
  styleUrl: './footer.css',
})
export class FooterComponent {
  // Componente base del pie de pagina.
}
